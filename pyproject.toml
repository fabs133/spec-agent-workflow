[project]
name = "spec-agent"
version = "0.2.0"
description = "Spec-Pattern Multi-Agent Workflow System (standalone)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
]
